// Este arquivo foi substituído pelo Prisma ORM
// Os seeds agora são gerenciados por:
// - prisma/seed.ts (arquivo de seed)
// - npm run seed (executar seed)

import seedData from './seedData.json';

function runSeeders(): void {
    console.log('✅ Seeds gerenciados pelo Prisma ORM (use: npm run seed)');
}

export { runSeeders };
