// Este arquivo foi substituído pelo Prisma ORM
// As migrations agora são gerenciadas por:
// - prisma/migrations/ (histórico de migrations)
// - npx prisma migrate dev (criar novas migrations)
// - npx prisma migrate deploy (aplicar migrations em produção)

function runMigration(): void {
    console.log('✅ Migrations gerenciadas pelo Prisma ORM');
}

export { runMigration };
